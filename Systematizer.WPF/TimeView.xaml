<UserControl x:Class="Systematizer.WPF.TimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Systematizer.WPF"
             mc:Ignorable="d" >
    <!-- DataContext is TimeVM-->
    <TextBox x:Name="eTime" Text="{Binding DisplayTime}" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus" IsReadOnly="{Binding IsReadOnly}"/>
</UserControl>
