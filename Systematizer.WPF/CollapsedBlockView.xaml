<UserControl x:Class="Systematizer.WPF.CollapsedBlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Systematizer.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600">
    <!-- DataContext is CollapsedBlockVM -->
    <DockPanel GotFocus="DockPanel_GotFocus">
        <TextBlock Text="{Binding Title}" Background="LightGray" Foreground="DarkSlateBlue" Cursor="Hand" MouseDown="Title_MouseDown"  Padding="22,2,2,5" FontSize="16" FontWeight="Bold"/>
    </DockPanel>
</UserControl>
